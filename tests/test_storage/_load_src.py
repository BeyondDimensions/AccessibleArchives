import os
import sys
if True:  # stop code editors from rearranging the following code
    SRC_PATH = os.path.abspath(os.path.join(
        __file__, "..", "..", "..", "src"
    ))
    sys.path.append(SRC_PATH)
